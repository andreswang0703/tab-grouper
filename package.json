{
  "dependencies": {
    "tldts": "^7.0.10"
  },
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "**Tab Grouper** is a minimal Chrome extension that helps you manage your tabs more efficiently using keyboard shortcuts. Group tabs by domain, include neighboring tabs, or ungroup with a single key press.",
  "main": "background.js",
  "devDependencies": {
    "esbuild": "^0.25.6",
    "tslib": "^2.8.1",
    "zip-a-folder": "^1.0.0",
    "fs-extra": "^11.2.0"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "esbuild src/background.js --bundle --outfile=dist/background.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
